@model IReadOnlyCollection<Marketplace.Web.Models.Cards.CityCardVm>

@if (Model != null && Model.Any())
{
    <div class="taxonomy-grid">
        @foreach (var item in Model)
        {
            <partial name="~/Views/Shared/Cards/_CityCard.cshtml" model="item" />
        }
    </div>
}